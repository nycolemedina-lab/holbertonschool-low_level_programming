#include <stdio.h>
/**
*main - entry point
*Return: Always 0 (succesfuly)
*/
int main(void)
{
printf(sizeof "of a"  char);
printf(sizeof "of an" int);
printf(sizeof "of a long int" long int);
printf(sizeof "of a long long int" long long int);
printf(sizeof "of a" float);

return (0);
}
