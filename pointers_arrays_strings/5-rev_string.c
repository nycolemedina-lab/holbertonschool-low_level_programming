#include "main.h"
/**
* rev_string - Reverses a string
* @s: pointer whith the string
*
* Returns: void
*/
void rev_string(char *s)
{
	int a = 0;
	int b = b - 1;

	while (s[a] != '\0')
	{
		while (b >= 0)
		{
			a = b;
			a++;
		{
		b++;
	}

}
