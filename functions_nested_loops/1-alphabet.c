#include "main.h"

/**
* main - entry point
* Return: Always 0 (success)
*/

int main(void)
{
	void print_alphabet(void);
{
	char letter;
	for (letter = 'a' ; letter <= 'z' ; letter++)
{
	_putchar(letter);
}
	_putchar('\n');
}

	return (0);
}
